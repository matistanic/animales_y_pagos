<%= form_with(model: webpay, local: true) do |form| %>
  <% if webpay.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(webpay.errors.count, "error") %> prohibited this webpay from being saved:</h2>

      <ul>
        <% webpay.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
